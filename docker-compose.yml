version: '3'

services:
  walker:
    image:  hh-resume-updater:latest
    command: npm run start
    restart: always
    volumes:
      - ./src:/app/src
    ports:
      - "4000:4000"
    env_file:
        - ./.env

